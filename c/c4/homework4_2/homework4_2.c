#include <stdio.h>
//sketch the Selecttion ranking method.
void main()
{
	printf("选择排序法就是在数列中,通过每一次循环,找到一个最值(最大或者最小),如果是升序就是找最小 如果是降序就是找最大.\n");
	printf("每个循环找到的这样的最值,都放到最前面 也就是这2个元素呼唤位置,\n");
	printf("每比较的结果都会找到最值,记录下最值得下标,如果这个下标不是数列剩余元素的第一个,那么就进行交换\n");
	
}